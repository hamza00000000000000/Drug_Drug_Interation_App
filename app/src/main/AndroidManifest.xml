<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mediguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MediGuide">
        <activity android:name=".Disease_Info_Activity"></activity>
        <activity android:name=".Interactions_info" />
        <activity android:name=".Dose_indication" />
        <activity android:name=".Interaction_screen_activity" />
        <activity
            android:name=".DashBoard"
            android:theme="@style/dashboard" />
        <activity
            android:name=".Splash_Screen_Activity"
            android:theme="@style/splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>